# a requirements file for `%pip install`     each line can be a comment, like this ...
dask                                         # a package name ...
traitlets >=5                                #   ... with a version specifier ...
-r ../../data/more-requirements.txt          # another -(-r)equirements file ...
pyzmq      ; sys_platform != "emscripten"    # a pep508 selector https://peps.python.org/pep-0508 ...
tensorflow ; platform_machine != "wasm32"    # other ones work, too ...
keras      ; platform_system != "Emscripten" # or even ...
# ...or a URL ending in (but generally not from PyPI because CORS) ending in:
# - py3-none-any.whl or
# - cp310-cp310-emscripten_3_1_14_wasm32.whl
