#!/usr/bin/env bash
set -eux

yarn
yarn build

# enable the proxy
for app in notebook server; do
    mkdir -p ${NB_PYTHON_PREFIX}/etc/jupyter/jupyter_${app}_config.d
    cp .binder/jupyterlite-proxy.json ${NB_PYTHON_PREFIX}/etc/jupyter/jupyter_{app}_config.d
done
